import express from 'express';

const router = express.Router();

// Example: Voice processing route
// router.post('/process-audio', audioController.processAudio);

// Example: Get voice analysis
// router.get('/analysis/:id', audioController.getAnalysis);

export default router;
